require "spec_helper"

describe Emque::Mailer do
  it "has a version number" do
    expect(Emque::Mailer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
